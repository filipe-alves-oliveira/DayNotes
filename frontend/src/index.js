import React from 'react';
import ReactDOM from 'react-dom/client'; //arvore de elementos, app para web
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <App />
);


//JSX = usar html no js, qdo usar html dentro do js, precisa importar o react.
